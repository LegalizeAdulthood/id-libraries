Colormap file collections from an archive snapshot of
spanky.triumf.ca.  Each collection was a single ZIP file.

Compare with the colormap files in the spanky library,
which are just enough to satisfy the parameter sets that
reference a colormap file.
